from setuptools import setup, find_packages

setup(
    name = 'SWAlgorithm',
    version = '0.1.4',
    keywords='SW',
    license = 'MIT License',
    author = 'Dingyao',
    packages = find_packages(),
    include_package_data = True,
    platforms = 'any',
    install_requires = [],
)
